import { DateDescriptionPipe } from './date-description-pipe';

describe('DateDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new DateDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
