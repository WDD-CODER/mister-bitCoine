import { InputColor } from './input-color';

describe('InputColor', () => {
  it('should create an instance', () => {
    const directive = new InputColor();
    expect(directive).toBeTruthy();
  });
});
